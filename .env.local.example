# Backend API Base URL
# Used for server-side authentication routes (login, logout)
BACKEND_BASE_URL=http://localhost:4000

# Optional: shown in API token curl examples (defaults to http://localhost:4000)
NEXT_PUBLIC_BACKEND_BASE_URL=http://localhost:4000
